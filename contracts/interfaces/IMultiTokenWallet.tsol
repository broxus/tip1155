pragma ever-solidity >= 0.62.0;

interface IMultiTokenWallet {
    /// @notice MultiToken info
    /// @return id Unique MultiToken ids
    /// @return collection Collection smart contract address
    function getInfo() external view responsible returns(uint256 id, address collection);
}
